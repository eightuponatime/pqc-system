[package]
name = "pqc-math"
version.workspace = true
edition.workspace = true

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "ntt_benchmark"
harness = false

[[bench]]
name = "simd_benchmark"
harness = false
